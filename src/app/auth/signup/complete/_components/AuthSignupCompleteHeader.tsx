import Header, { HeaderCenter, HeaderTitle } from '@/components/layouts/Header'

export default function AuthSignupCompleteHeader() {
  return (
    <Header variant="white" height={55}>
      <HeaderCenter>
        <HeaderTitle>회원가입</HeaderTitle>
      </HeaderCenter>
    </Header>
  )
}
